- [Git working-tree wipe + recovery](git-tree-wipe-incident.md) — auto-commit `git add -A` will commit a full-tree deletion with no guard; recover via git (not cp); don't weaken the publish guard to hide an empty index.
