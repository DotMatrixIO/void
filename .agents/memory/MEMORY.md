- [Git working-tree wipe + recovery](git-tree-wipe-incident.md) — auto-commit `git add -A` commits a full-tree deletion; now backstopped by a tracked-file-count floor; recover via git not cp; don't weaken the guard to hide an empty index.
- [video-js timed-animation verification](video-js-timed-animations.md) — to screenshot a later phase, swap the phase hook for a const (forces remount past Fast Refresh state), repoint SCENE_COMPONENTS.open, bust cache with ?v=/viewport; always revert.
- [Audio-leak verify headless](audio-leak-verify-headless.md) — verify AudioContext teardown via the self-contained tools/audio-leak-verify harness; real void routes create 0 contexts headless (vacuous); paths converge on one closeAudioContext.
- [Demo poster capture](demo-poster-capture.md) — regenerate landing demo posters from live video apps via `gen:demo-posters`; puppeteer gotchas: no networkidle0 (streaming video hangs), abort vite on early exit or you screenshot a stale orphan → blank PNG.
- [Biometric MP4 record](biometric-mp4-record.md) — biometric-video-drift watches ALL src incl. VideoWithControls (non-recorded), so re-export anyway; `rm -rf /tmp/video-captures` first or record-biometric.mjs encodes a stale webm → identical MP4 → drift fails as UNCHANGED.
- [Demo-video fixed-canvas scaling](demo-video-fixed-canvas.md) — scenes are pixel-tuned for 1280×720; scale the whole canvas to fit fluid embeds (don't patch font sizes); vw/vh inside the canvas break under scaling — use canvas-px.
- [Replit preview navigation](replit-preview-navigation.md) — inside the proxied preview iframe a full nav to a constructed BASE_URL strands the user on a blank page; use replaceState + location.reload() (current doc) to reset & go home.
- [void-client copy/color CI guards](void-client-copy-guards.md) — user-facing copy/color edits must pass the marketing-voice checks (banned phrases, required literals, contrast token audit); run them before completing.
- [VOID "stateless" claim scope](void-stateless-claim-scope.md) — accurate claim is "no accounts and no room content stored"/"ephemeral" (§3.5 paid-room snapshot survives restart); marketing guards DON'T scan root README.md.
- [void-client route/copy-rename guard fan-out](void-client-route-rename-guards.md) — new route needs a VOID_TECHNICAL_OVERVIEW.md §6.2 row; renamed button copy must be updated in vitest AND both tests/playwright specs (exact-string names).
- [launch-links gate vs gitignored evidence](launch-links-gitignored-merge.md) — task-agent fixes under gitignored attached_assets/ don't survive merge; main stays red but it never blocks publish.
- [backdrop-filter vs opacity layers](backdrop-filter-opacity-layer.md) — large opacity<1 overlays break nearby backdrop-filter text (vanishes until repaint); bake alpha into rgba() instead of using opacity.
- [Coordination demo MP4 render](coordination-demo-render.md) — Playwright recordVideo captures no audio; mux voiceover.mp3 separately, trim lead-in via window.startRecording hook so MP4 t=0 == scene-0 start.
- [ffmpeg atrim+afade order](ffmpeg-atrim-fade-order.md) — afade after atrim mutes the segment (fades use original PTS); always asetpts=N/SR/TB before afade. Verify with astats (peak -inf = dead).
- [media play() rejection overlay](media-play-rejection-overlay.md) — every video/audio .play() in demo-video artifacts needs .catch(); uncaught reject → Replit runtime-error overlay "no supported sources" on unmount/scene-end.
- [Playwright engines on Replit](playwright-engines-replit.md) — firefox isn't pre-installed (chromium+webkit are); headless WebKit Linux can't do WebRTC ICE — skip live-WebRTC there, cover Safari manually.
- [Fake .onion origin across engines](onion-origin-fake-webkit.md) — replace Chromium-only --host-resolver-rules with page.route() re-fetch to 127.0.0.1 so WebKit too sees the fake hostname; clipboard perms are Chromium-only.
- [void-client autofocus test race](void-client-test-autofocus-race.md) — StartScreen join mode arms a 50ms grid-slot autofocus; under load it steals focus mid-test → use fireEvent.change(target), not click+keyboard.
- [void-client a11y patterns](void-client-a11y.md) — SAS announce = natural words only (no spell/NATO); axe-in-jsdom is component-scoped (src/test/axe.ts); role=menu needs ALL children menuitem; axe pass ≠ SR-accessible (hand off manual checklist).
- [Device-cloud Safari WebRTC](device-cloud-safari-webrtc.md) — automate real Safari WebRTC via Playwright webkit.connect to a device cloud; macOS Safari is the portable default (Playwright can't drive real iOS); skip cleanly when env unconfigured.
- [signaling-envelope audit guard](signaling-envelope-audit-guard.md) — new socket events/data-channel labels need a doc row in `docs/signaling-envelope-audit.md` + whitelist entry; also rebuild signaling-types dist after codegen or typecheck reads stale .d.ts.
- [Overview envelope enumeration drift](overview-envelope-enumeration-drift.md) — the overview's "N events / M labels are: …" prose is hand-counted & CI-unchecked (was already stale); update it by hand when audit counts change.
- [signaling scanner needs literal createDataChannel](signaling-channel-scanner-literal.md) — check-signaling-envelope.mjs only matches LITERAL createDataChannel("…"); a label passed as a constant evades the guard and ships undetected.
- [VOID relay-verify single-host](void-relay-verify-single-host.md) — single-host coturn never selects relay/relay (base sockets reach relay ports directly → verifier fails/null.send crash); CI fix = per-peer netns + relay-ip≠base-IP, peers wrapped via SMOKE_WRAP_<ROLE>, harness as root.
- [VideoSlot muted + isolated render stubs](void-client-video-muted.md) — set el.muted imperatively (React muted prop unreliable); receiver-side peer mute in PeerTileGrid; jsdom render needs play/AnalyserNode/ResizeObserver stubs.
- [License headers vs media drift](license-headers-vs-media-drift.md) — repo-wide SPDX/comment codemods trip byte-exact media-drift guards (biometric-video-drift, still-poster-drift); carve out artifacts/biometric-demo-video/src before any header pass.
- [Lockfile regen vitest drift](lockfile-regen-vitest-drift.md) — fresh pnpm-lock is version-neutral yet broke void-client-tests (jest-dom can't resolve vitest peer); FIX = pnpm.packageExtensions declaring vitest as optional peer of @testing-library/jest-dom.
- [VOID public-release CI fork-gating](void-public-release-ci-gating.md) — gate canonical-only jobs with `github.event.repository.fork == false` (works pre-slug); unreliable on schedule events — pin to repository_owner there.
- [void-client typecheck gap](void-client-typecheck-gap.md) — void-client-tests builds via Vite (no tsc) and only sdk-typecheck runs in CI; app-level `tsc` errors ship undetected — run typecheck manually when editing void-client TS.
- [void-client-tests pre-failing helloEnvelope](void-client-tests-prefailing.md) — a red full suite with failures only in src/lib/helloEnvelope.test.ts is the agent-protocol layer, unrelated to UI/palette diffs; scope-test your own file before chasing it.
- [void-client reconnect test env-blocked](void-client-reconnect-test-prefailing.md) — ONION-budget reconnect test fails under Node-version skew; env-blocked, not a CI/docs-diff regression.
- [demo-video typecheck gates](demo-video-typecheck-gate.md) — each demo-video artifact has a `<name>-typecheck` validation; missing DOM lib caused BOTH window/document AND framer-motion Variant errors — add `dom` lib before casting variants.
- [api-server typecheck & composite refs](api-server-typecheck-references.md) — bare `tsc -p` + `references` reads a lib's stale committed `dist/*.d.ts`, not src; `tsc -b` is mtime-fragile; fix = drop references so exports resolves to source.
- [VOID rekey late-answer desync](void-rekey-late-answer.md) — responder commits optimistically on send; initiator must retain its ephemeral key with NO answer-timeout (channel is reliable+ordered); clear pending on onclose, not a timer; "resend answer" can't recover.
- [VOID SESSION vs ROOM vocab](void-session-vs-room-vocab.md) — settled three-term model: ROOM = paid space (host/join/burn/expire), call = live conversation, session = crypto term only; runtime UI migrated off "SESSION".
- [smoke-serve-static load flake](smoke-serve-static-load-flake.md) — "Server exited before listening (code=0)" only under mass concurrent workflow restart; passes in isolation; not a serve-static code defect — re-run before chasing.
- [Concurrent validation resource exhaustion](concurrent-validation-resource-exhaustion.md) — full-suite mark_task_complete saturates the container: vitest EAGAIN/pthread_create, playwright "browser closed", api-server backend timeouts despite all test counts green — flake, not your diff.
- [void-client beige --bg literals](void-client-beige-token.md) — page beige is duplicated as raw literals (body gradient, .void-overlay, PaywallModal scrim/QR); change in lockstep with --bg; beige-as-text on dark is a separate role.
- [Main agent destructive-git guardrail](main-agent-git-guardrail.md) — main agent is hard-blocked from ALL .git-mutating ops (git rm --cached, git config --local, even rm .git/*.lock), even reversible ones; defer to a dedicated git task; .gitignore alone won't untrack.
- [doc-code-drift guard](doc-code-drift-guard.md) — check:doc-code-drift pins GC_INTERVAL_MS + SDP codec allowlists to docs; codec list duplicated in OVERVIEW §14 AND INTERNALS (lockstep); anchor before "audio:".
- [Access-log URL secret leak](access-log-url-secret-leak.md) — redacting a secret from log FIELDS misses the access logger's req.originalUrl; scrub by route position (any charset/case), not a fixed-shape regex, since backends accept varying ID shapes.
- [TOR_ONLY runtime posture](tor-only-posture.md) — TOR_ONLY=1 now keys real behavior (lib/torOnly.ts): suppress STUN in /api/ice-servers both branches, startup banner, warn on non-turns:/.onion TURN; docs say so.
- [pnpm overrides location](pnpm-overrides-location.md) — pnpm reads overrides from root package.json `pnpm.overrides`, NOT pnpm-workspace.yaml's `overrides:` (silently inert); cap CVE floors to the patched major (`^x.y.z`) or you jump a major.
- [CVE appendix moderate fixes & live-strict gate](cve-appendix-moderate-fixes.md) — fixed Moderate/Low advisories get audit-doc strikethrough rows but NO AUDIT_LEDGER entry (ledger is High/Critical-only); `--strict`/`--check` run a LIVE pnpm audit, so a same-week High disclosure reddens the gate with no lockfile change.
- [logrotate real-rotation smoke test](logrotate-smoke-test.md) — maxage keys on mtime not dateext name; strip `su root root` for non-root; glob-rewrite guard can't substring-match `/var/log/void/*.log` (logDir ends in it).
- [VOID create-room JWT claim fan-out](void-create-room-jwt-claim-fanout.md) — create-room replay-guard JWT claim is a random jti (NOT paymentHash, which stays server-side); change it and ALL inline-minted test tokens must migrate or create-room → PAYMENT_REQUIRED.
- [VOID env-var audit bracket notation](void-env-audit-bracket-notation.md) — api-server reads env via process.env["X"]; dot-only grep false-negatives TURN/PAYWALL/ONION; StartOS build-readiness audit in docs/manifest-review-2026-06.md §6–§7.
- [void-client/api-server validation env flakes](void-client-validation-env-flakes.md) — helloEnvelope.test fails in jsdom (Ed25519 unsupported); api-server lightning/KMS/WebRTC env-only; skip for UI-only diffs.
- [proof endpoints excluded from openapi spec](proof-endpoints-not-in-spec.md) — /api/proof/* routes are NOT in openapi.yaml; overview-http-drift does not gate them, so new proof routes need no spec/overview edit.
- [onion v3 validation + build guard](onion-v3-validation.md) — onionHost.ts is the ONE v3 check (56 base32) shared by origin.ts runtime + vite.config build guard; tightening it fans out across ~8 test-file fixtures.
- [VOID org identity & publish rename](void-org-identity-rename.md) — canonical = DotMatrixIO/void; rename LIVE refs only (README*/SECURITY/manifests/repo.ts+test/proof-build.ts+test in lockstep); leave docs/ to the history-scrub task.
- [VOID already public — incremental updates](void-already-public-incremental-update.md) — DotMatrixIO/void is LIVE; §3 first-publish snapshot is spent; ongoing releases = clean commit on top of public main (clone→rm→archive→pseudonymous commit→non-force push); gitleaks non-zero is expected (custom rules force triage).
- [StartOS compat real-tool verify](startos-compat-real-verify.md) — Docker works here; run REAL `start9/compat` config get/set against assets/config_spec.yaml then through docker_entrypoint.mjs; `~` nulls, colon-only quoting, defaults baked in; .s9pk/UI/on-device still gated.
- [publish-doc-hygiene guard](publish-doc-hygiene-guard.md) — scans docs/**/*.md for Void-PWA + .local/ leaks; carve out PRIVATE docs AND hazard-procedure ledgers (pre-publish-scrub) that quote the hazards by design.
- [security-key expiry guard verification](security-key-expiry-verify.md) — the security-contact key-expiry guard can't be exercised against the real key in CI; forge fixtures by editing a signature packet (fingerprint is over the pubkey packet only, so it stays valid), no gpg needed.
- [CVE appendix regen flow & strict gate](cve-appendix-regen-flow.md) — appendix auto-regens via regen-and-commit-on-main; never hand-regen mid-task; EXCEPT you MUST regen when changing an embedded docHref/header without touching the lockfile (diff must be only your string + hash); --strict fails on any fresh unmapped High/Critical.
- [Publish internal audit → public copy cross-links](audit-doc-publish-cross-links.md) — the published copy is itself a scanned shipping doc, so verbatim content citing the internal doc trips check:publish-cross-links; legit residual hits = PRIVATE docs + the guards' own never-ship lists.
- [Async join breaks microtask-only test pumps](async-join-breaks-microtask-pump.md) — adding crypto.subtle (HKDF) to a flow makes its promise resolve on a real macrotask; microtask-only "pump until emit" helpers flake under fake timers — yield to a real captured setTimeout.
- [wire-core boundary](wire-core-boundary.md) — neutral wire primitives live in @workspace/wire-core; void-client/api-server/wire-core must never import @workspace/agent-protocol; enforced by check:publish-boundary.
- [Permanent named CI guard vs workflow cap](replit-workflow-cap-validation.md) — configureWorkflow caps new adds at 10 & .replit is uneditable; register permanent named guards via the validation skill (setValidationCommand).
- [VOID agent removal](void-agent-removal.md) — agent SDK/protocol/rooms/Agent-Mode deleted (v0.6); crypto survivors (argon2/brand/hello-envelope) MOVED to lib/wire-core; reconcile dated audit docs by tombstoning removed sections (no delete/renumber), not by inventing replacements.
- [artifact.toml service env table](artifact-toml-services-env.md) — service env (BASE_PATH) must sit under a `[services.env]` table; bare keys after `[[services.production.rewrites]]` silently bind to the rewrite, not the service; PORT auto-injects so only BASE_PATH breaks.
- [Replit git identity injection](replit-git-identity-injection.md) — Replit stamps wrong commit identity 2 ways: per-user config (/run/replit/user/<id>/.config/git/config) = real name on fresh repos; Git pane/checkpoint = Replit Agent. Beat both with inline GIT_AUTHOR_*/COMMITTER_* in a plain shell.
- [VOID §4 publish secret-scan triage](void-publish-secret-scan-triage.md) — gitleaks allowlist by VALUE not path; hazard grep needs `-rIniE` + `\b`; shipped guards+runbook are legit residuals, not "nothing".
- [VOID pre-publish inventory guard](void-publish-inventory-guard.md) — publish scrub is a fail-open denylist; check-publish-inventory.mjs fail-closes it: SOURCE mode (git ls-files) + SNAPSHOT mode (--snapshot $PUB) make the manifest the single source of truth.
- [Self-host Docker build](selfhost-docker-build.md) — corepack 0.34.5 pin (3 places); staged Dockerfile needs docs/ + tsconfig.base.json + ALL lib/ SOURCE + every workspace pkg.json; runtime NODE_ENV must stay production (bundled image has no node_modules → pino-pretty crashes); .env NODE_ENV drives BUILD onion guard only.
- [release.yml onion-host threading](release-onion-threading.md) — VITE_VOID_ONION_HOST from a repo VARIABLE must be threaded identically into all 4 prod build paths + docker build-arg or the reproducibility diff breaks; unset = fails closed.
- [useRoomConnection reconnect ONION-budget flake](void-client-reconnect-onion-budget-flake.md) — timing test fails ~1/10 in full+isolated runs; if it is the ONLY red on a copy/UI diff, it is not yours.
- [publish-inventory manifest drift](publish-inventory-manifest-drift.md) — post full-tree recovery, UNCLASSIFIED attached_assets (legacy tracked .txt) + STALE replit.nix (absent from tree) are GENUINE drift, not empty-index artifacts; owned by publish/manifest task, not recovery.
